# 2026-08-05 工作日志

## Tier 1 重构：引入 imgui_toggle + implot 替代手绘开关/折线
- 新增 `ThirdParty/imgui_toggle/`（cmdwtf/imgui_toggle，0BSD）：头文件 imgui_toggle.h/.cpp + _math/_palette/_renderer(_.h/.cpp) + imgui_offset_rect.h 共 9 个文件。注意实现分散在多 cpp，需全部编入 NativeApp；vendored 副本 `imgui_toggle_renderer.cpp` 删了一行未使用变量 `width` 以满足零警告。
- 新增 `ThirdParty/implot/`（epezent/implot 1.1 WIP master，兼容 imgui 1.92.8）：implot.h/.cpp + implot_internal.h + implot_items.cpp。
- `xmake.lua`：NativeApp 加入两个库的 include 与 add_files（imgui 仍为独立共享库 libimgui.{dylib,so}，implot 编入 NativeApp）。
- `Frontend/Frontend.cpp::init`：在 `ImGui::CreateContext()` 之后显式 `ImPlot::CreateContext()` + `ImPlot::SetImGuiContext(ImGui::GetCurrentContext())`，解决 imgui 跨 dylib 共享上下文问题。
- `SettingsScreen.cpp`：删除手绘 `DrawToggle`，改用 `ImGui::Toggle`；用静态 `ImGuiTogglePalette`（开=青蓝(80,180,255)/关=灰(80,90,105)、旋钮白）复刻原观感；新增 `DrawSettingToggle` helper 统一 4 处开关定位。
- `RealtimeScreen.cpp::renderTrendHero`：用 implot `BeginPlot/SetupAxis(NoDecorations)/PlotLine(ImPlotSpec 设 LineColor=accentCyan、LineWeight=2.5、Circle Marker)` 替代 ~45 行手绘归一化+折线+端点；deque→vector 供连续访问；PlotBg/FrameBg 设为 (30,41,59) 融入卡片；flags=`CanvasOnly|NoFrame`。
- 验证：`xmake f -m releasedbg && xmake` 零错误、两新库在 -Wall/-Wextra 下零警告（仅既有 StyleManager -Wswitch 与 spdlog/fmt deprecation 豁免，均不在本次改动范围）；启动 4s 无 implot 断言/崩溃。
- 待用户目视确认：macOS 跑 `open pkg/NativeApp.app`，检查设置屏开关（开=青蓝、关=灰、旋钮滑动动画）与趋势屏 implot 折线（青蓝线+圆点、深色底、无坐标装饰）。

## Tier 0 重构：收口手绘控件到 UIHelpers（零外部依赖）
- 新增 `Frontend/UIHelpers.h` + `Frontend/UIHelpers.cpp`（namespace `UI`），集中实现：
  - `CenteredText` / `DrawRoundedRect` / `DrawSegmented`(含 `SegmentedStyle` 风格结构体) / `accentCyan` / `DrawRing` / `DrawGaugeArc`
- 复用改造：
  - `NavBar.cpp`：胶囊导航改用 `UI::DrawRoundedRect` + `UI::DrawSegmented(drawBg=false, inset=4)`（图标标签合并进字符串，单 helper 覆盖三处分段）
  - `RealtimeScreen.cpp`：指标切换→`DrawSegmented(gap=4,hover)`；海拔圆环→`DrawRing`+`DrawGaugeArc`；气压/温度装饰环→`DrawRing`；趋势图底框→`DrawRoundedRect`；卡片→`DrawRoundedRect`；删除本地 `CenteredText` 与成员 `accentCyan`（改 `UI::`）；`RealtimeScreen.h` 同步移除 `accentCyan()` 声明
  - `SettingsScreen.cpp`：删除本地 `DrawSegmented`/`BeginRow`，改用 `UI::DrawSegmented`(segStyle bg=16,22,32 gap=2) 与 `UI::DrawRoundedRect`；保留 `DrawToggle`（Tier 1 再换 imgui_toggle）
  - `RecordsScreen.cpp` / `MapScreen.cpp`：统计卡/轨迹卡/地图底框/坐标卡背景改 `UI::DrawRoundedRect`（地图等高线/路线/定位针保持手绘装饰，无合适轮子）
- 构建：`xmake f -m releasedbg && xmake` 通过，本次改动零警告（第三方 spdlog/fmt deprecation 除外）
- 已知遗留（非本次引入、不在范围内）：`Frontend/StyleManager.cpp:20` 的 `-Wswitch` 枚举未全覆盖警告，clean build 才显现；Tier 0 增删文件均为增量编译路径，未触碰该文件
- 验证方式：macOS 必须 `open pkg/NativeApp.app` 目视确认四屏+导航无视觉回归（自动化无法验证 GUI）
